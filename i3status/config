general 
{
    output_format = "i3bar"
    colors = true
    interval = 5
}

order += "cpu_usage"
order += "tztime local"
order += "disk /"

disk "/" 
{
    format = "DISK: %used/%total"
}

tztime local 
{
    format = "%l:%M:%S %p %Y-%m-%d"  # 12-hour format with AM/PM
}
